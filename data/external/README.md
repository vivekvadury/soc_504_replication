# External data directory

No manual external runtime files are currently required for the standalone replication workflow.

This directory is reserved for future additions if a non-redistributable input becomes necessary.
